import React from "react";
import BadgedButton from "../../../../buttons/BadgedButton";

const TwoButtons = () =>

  <div>
    <BadgedButton onClick={() => { }} iconName="timeBack" iconWidth={15} iconHeight={15} />
    <BadgedButton onClick={() => { }} iconName="itemList" iconWidth={15} iconHeight={15} />
  </div>;

export default TwoButtons;
